# Project Progress

Project: RunSmart Web
Status: Active
Current Phase: **Active** (2026-09-23: all projects are equally important; the 2026-07-02 maintenance-only rule is retired). Next: fix the three live P0s, then a spin-off brainstorm (STORM + Red Team) on what RunSmart's engine can become. Garmin stays paused on its own evidence: the app was deactivated and all 9 connections read `reauth_required`.
Active Story: **None, but three P0s are open and unfixed.** A CTO review of RunSmart Web on 2026-09-12 (filed 2026-09-19) found three defects live on production, each reproduced with an unauthenticated request: `POST /api/chat` returns a streamed GPT-4o completion to any anonymous caller billed to our OpenAI account (the `Map`-backed rate limiter in `lib/security.config.ts:189` is per-lambda-instance on Vercel and is not a control; the route also accepts `role:"system"` from the client body); `GET /api/test-openai-direct` executes a real OpenAI call per anonymous request and returns `"apiKeyPrefix":"sk-proj-"`, while `/api/debug-env` returns the Supabase project ref; and `posthog.identify()` fires only from `components/auth/signup-form.tsx:134`, never on login, under `person_profiles:'identified_only'`, so returning users carry no person profile and the D30 >=40% target is not computable from the data being collected. Plan: `docs/specs/2026-09-19-web-p0-hardening-plan.md` (PR #132). Nothing is implemented yet. Separately, WP-72 security lane closed and the suite is fully green. **The full vitest suite now passes 1418/1420 with zero failures** (2 intentionally skipped), the first clean run since 2026-07-10. Garmin relaunch work stays paused. See Nadav Builder OS vault `05-Decisions/2026-07-02-priority-reset-resumely-primary.md` and Agentic OS `DECISIONS.md`.
Last Completed Story: 2026-08-08 — **WP-72 Story 1: closed the one internet-reachable dependency CVE.** Risk triage (Agentic OS `executive-os/reviews/2026-08-07-risk-review-runsmart-web-dependency-gate.md`, PR #61) established that of the 13 critical/high audit findings, exactly one is reachable from the public internet: `POST /api/ai-activity` is unauthenticated (`middleware.ts` skips all `/api/*` except `/api/auth`) and pipes a ≤6MB user-supplied image into `sharp@0.34.5` / libvips 8.17.3, which carries GHSA-f88m-g3jw-g9cj (CVE-2026-33327/33328/35590/35591). Bumped `sharp` `^0.34.5` → `^0.35.3` (libvips 8.18.3); lockfile diff confined to the `@img/*` + `@emnapi/runtime` sharp family, no other package moved. Because `preprocessActivityImage()` returns early when `NODE_ENV === "test"`, this path has **zero** automated coverage, so it was verified with an A/B harness running the exact pipeline on both versions: output contract identical (JPEG/PNG/WebP format, 1600px width, channels, colourspace, `withoutEnlargement`, EXIF-orientation rotation, garbage-input rejection). One behavioural difference: a truncated JPEG was silently tolerated on 0.34.5 and now throws a catchable `Error`, which the route's outer catch turns into a clean JSON 500 (no crash, no stack leak). 2026-07-03 — Supabase aggregate check on `garmin_connections` found all 9 rows now `reauth_required`, 0 `connected` (newest successful sync `2026-07-01 03:40:58`, just before the app deactivation) — the "keep the 5 currently-synced users working" premise behind the maintenance-mode decision is stale; there are 0 users currently syncing, not 5. Verified the reconnect UX for affected users is not silently broken: `handleGarminConnect()` in `profile-screen.tsx` correctly surfaces WP-25's gated 503 message as a toast. Fixed one in-scope papercut: that toast was titled "Connection failed" for this planned-pause case, which reads more alarming than warranted; now shows "Garmin sync paused" (default variant) when the error matches `GARMIN_CONNECT_DISABLED_MESSAGE`, "Connection failed" (destructive) for genuine errors. `npx tsc --noEmit` clean, `npx eslint components/profile-screen.tsx` 0 errors/12 pre-existing warnings (none new), `profile-screen.test.tsx` 1/1 passed.
Next Recommended Story: **STORY 0 of the P0 hardening plan — set an OpenAI budget cap.** Five minutes, no code, and it is the only control that works while the auth work is in progress. Then STORY 1 (delete `app/api/{test-openai-direct,debug-env,chat-test,test-chat,test-onboarding-chat}/`) and STORY 2 (one shared auth helper across 11 OpenAI routes, with `/api/onboarding/chat` explicitly carved out because anonymous onboarding calls it). Previously this line read "Nothing blocking", which was true for the eval track and wrong for the app. Optional follow-ups, none urgent: `middleware.ts` is deprecated in Next 16 in favour of `proxy.ts` (warning only, and the rename touches auth-redirect routing); `eslint-config-next` is held at 15.5.23 pending an ESLint 9 flat-config migration. Also open: `lib/activation-loop.test.ts` is FIXED (2026-08-08). It was date-dependent and failed every Thursday and Saturday: the fixture passed `trainingDays: [today, 'Thu', 'Sat']`, which `resolveStarterTrainingDays` dedupes to 2 distinct days when today is Thu or Sat — below `daysPerWeek: 3` — so it discarded the preference and fell back to the Mon/Wed/Sat default. On Thursdays that left no workout today at all (`getTodaysWorkout` → null); on Saturdays it served the 4km long run, and the test's flat +120s overage became a pace delta of exactly 30 s/km against a strictly-greater-than 30 threshold. Fixed in the test only (production logic is correct): the overage is now expressed as a distance-independent 60 s/km pace delta, and the training days are built to always contain 3 distinct days including today. Verified against all 7 weekdays with a Date-only fake-timer harness. Garmin: **still paused.** Restoring actual sync for the 9 reauth_required users needs either a working production/commercial credential set (WP-26 Steps 3-4) or pointing real users at the Evaluation-tier Internal Test app (the same Terms violation that got the old app deactivated) — there is no maintenance-mode-compatible fix available. This is a fact worth surfacing at the day-30 revisit (~2026-08-01), not a reason to resume now. See Agentic OS WP-26/27/28 for the paused relaunch scope.
Estimated Completion: N/A while paused. Credential guard + connection gate (WP-25) and the Internal Test app (WP-26 Step 2) remain the only completed Garmin infrastructure; everything past that stays parked.
Decisions: 2026-06-25 — Training API workout-push feature parked, not built. Founder's call after seeing the Cursor-session spec: building real Garmin workout-push requires Garmin's proprietary Training API contract details, which aren't publicly documented and would require petitioning the same Partner Services team gatekeeping Production approval. Not worth blocking the Gate-4 reply on, and not core to the roadmap at current traffic. Training API will be removed from Developer Portal scope; revisit only once usage volume justifies reopening that conversation with Garmin. `16-WORKOUT-PUSH-PLAN.md` kept as reference, marked parked. 2026-07-02 — Garmin relaunch (commercial app, real-device screenshots, reconnect flow) paused in favor of Resumely's 30-day activation push; see decision doc above.
Blockers: **No founder action outstanding on the eval track.** The `OPENAI_API_KEY` repo secret was added 2026-07-21 and the plan-generator nightly has passed **30 of its last 30 runs** (2026-07-29 → 2026-08-27, zero failures). The previous "Founder action required — add the repo secret" text stood here for five weeks after it stopped being true and was propagating into the Agentic OS priority board as the highest-scored next action; corrected 2026-08-28. Garmin relaunch work is paused by decision, not blocked on founder action. `GARMIN_TEST_CLIENT_ID` / `GARMIN_TEST_CLIENT_SECRET` remain intentionally absent from production; the WP-26 Internal Test app credentials stay non-production only, per WP-25's credential guard.
Risks: **Materialized, not hypothetical** — all 9 previously-connected users now show `reauth_required`, 0 `connected` (confirmed 2026-07-03, see Last Completed Story). No fix available without either resuming the paused commercial-app track or violating Garmin's Evaluation-tier Terms again. Migration drift remains: production has ~26 applied Supabase migrations with no matching file in v0/supabase/migrations/ (separate cleanup story).
Last Validation: 2026-09-19 — **`npx tsc --noEmit` exit 0 with zero errors (35s) and `npm run lint` exit 0** (warnings only, all pre-existing), after removing 45 iCloud sync conflict-copies from `node_modules/@types/`. Before that cleanup tsc ran 50+ minutes and exited 2 with 45 `TS2688: Cannot find type definition file for 'babel__core 2'`-style errors — phantom `<package> 2`/`<package> 3` directories that TypeScript auto-includes and cannot resolve. `find node_modules/@types -maxdepth 1 -type d -regex '.* [0-9]+$' -print0 | xargs -0 rm -rf` is the fix; 1,286 such conflict-copies exist across all of `v0/node_modules`, and `npm ci` is the clean full remedy. `node_modules` is gitignored so this is not captured in any commit — re-run the find before investigating a build failure of this shape. **Separately, the full `vitest run` is NOT green: 4 files failed, 2 tests failed, 1 unhandled error; 1400 passed / 2 skipped across 179 files, 3436s.** This supersedes the 2026-08-08 "1418 passed / 0 failed" claim later in this line, which no longer holds. Breakdown: (1) `lib/userInsightService.test.ts` asserted a fixed millisecond delta against `setDate()` calendar math and failed by exactly one hour because now + 8 weeks crosses the end of Israel Daylight Time on 2026-10-25 — production logic is correct, the test was wrong, **fixed in this commit** (18/18 pass, 2.51s); lesson recorded in `tasks/lessons.md`. (2) `lib/habitAnalytics.test.ts` and `lib/reminderService.test.ts` both died at module load with `[vitest-worker]: Timeout calling "fetch"/"resolveId"` on `dexie` and `posthog-js`, not on any assertion. (3) `lib/integrations/garmin/service.test.ts` timed out at the 5000ms `testTimeout`. Those last three are consistent with the documented File Provider content-read stalls under `~/Documents` (`collect` alone took 4918s) and are why the 2026-08-30 guidance to move node repos off the synced path stands. **Measurement hygiene:** do not read a slow or timing-out suite as a code defect before checking the machine is otherwise idle — a 2788s two-file run earlier in this session was contamination from two concurrent full-suite runs, and a clean control run of those same two files is 15.77s, 31 passed, 0 failed. `~/Documents` IS iCloud-managed (`defaults read com.apple.finder FXICloudDriveDesktop` returns 1); do not re-derive the opposite from an inode comparison, which is invalid under Desktop & Documents sync. Prior entry follows. 2026-08-28 — **eval track only**: `gh secret list` shows `OPENAI_API_KEY` present since 2026-07-21T12:07:55Z, and `gh run list --workflow=eval-plan-nightly.yml --limit 30` returns 30 of 30 `success` (2026-07-29 → 2026-08-27). Nothing else in this file was re-verified on that date. 2026-08-08 — **full vitest suite 1418 passed / 0 failed / 2 skipped (179 files), exit 0** — the first fully green run since 2026-07-10. `npm run lint` exit 0; `npx tsc --noEmit` exit 0; `NODE_ENV=production npm run build` exit 0; `audit-ci` passes at BOTH the `high` and `--moderate` thresholds with a zero-entry allowlist; `npm audit` 4 low, 0 moderate/high/critical. The activation-loop fix was verified across all 7 weekdays. Live production smoke test on `www.runsmart-ai.com` after the WP-72 merges: `/`, `/landing`, `/challenges/start-running`, `/api/og`, `/weekly/2026-W12` all HTTP 200 on Next 16, and `/api/ai-activity` rejects malformed input with a clean JSON 500. 2026-07-03 — see Last Completed Story for that day's checks. 2026-07-02 — credential-guard focused tests passed, 13 tests. Connection-gate focused Garmin suite passed, 36 tests; `npm run type-check` passed; targeted `npx eslint` on changed TS/TSX files exited 0 with 14 existing warnings in `components/device-connection-screen.tsx` and `components/profile-screen.tsx`. Read-only Vercel production env listing confirmed no `GARMIN_TEST_CLIENT_ID` / `GARMIN_TEST_CLIENT_SECRET`, no `GARMIN_CONNECT_ENABLED`, and no production env rotation.
Latest QA Report: PR #101 — CodeRabbit found 2 real correctness issues + 1 doc-accuracy issue + 1 deferred nitpick (DB-level CHECK constraints, reasonable to skip since app-level clamping already enforces 0-100 and columns are already live); all addressed before merge.

<!--
Seeded 2026-06-12 per Agentic OS MANUAL.md "How to make a project reach High confidence"
(open decision in Agentic OS tasks/progress.md, approved by founder 2026-06-12).
The app lives in v0/ — run lint/type-check/test from there. Keep the keyed block above
current after each significant validation; the Agentic OS refresh parser reads it.

Note found during seeding: v0/node_modules was inconsistent with package-lock.json
(eslint-scope missing, lint crashed). Fixed with npm ci on 2026-06-12. If lint crashes
with "Cannot find module", run npm ci in v0/ first.
-->
Last Updated: 2026-09-19
