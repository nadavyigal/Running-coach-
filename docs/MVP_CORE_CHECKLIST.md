# MVP Core Checklist (Dexie-only)

- [x] Onboarding wizard completes on a fresh database
  - [x] Console logs show `✅ Onboarding complete - userId, planId`
  - [x] 12 workouts are created
- [x] Today screen renders after onboarding
  - [x] Refresh keeps user on Today (onboardingComplete=true)
- [ ] Plan generation baseline works without external services
- [ ] AI Chat gracefully falls back when no `OPENAI_API_KEY`
- [ ] Quick Start steps verified (README)

When all items are checked, capture a screenshot/GIF and store it under `docs/implementation-reports/`.

