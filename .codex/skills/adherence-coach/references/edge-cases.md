# Edge Cases

- Vacation/blackout days → avoid scheduling; if impossible, request more availability.
- User requests catch-up marathons → refuse and emit `SafetyFlag: load_spike`.
- Injuries in notes → stop catch-up, shift to recovery guidance.
