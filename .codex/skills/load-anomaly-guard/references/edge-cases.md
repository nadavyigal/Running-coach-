# Edge Cases

- Missing distance data → emit `missing_data` and avoid automatic adjustments.
- Race week → allow slight spikes but still recommend recovery days post-race.
- Back-to-back intensity days → flag even if total volume stable.
